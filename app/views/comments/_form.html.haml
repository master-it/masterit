= simple_form_for @new_comment , :html => {:class => "form-horizontal"}do |f|
  = f.input :body, label:false, :as => :text, :input_html => { :rows => 3}
  = f.hidden_field :work_id, value: @work.id
  = f.hidden_field :user_id, value: current_user.id
  .form-actions
    = f.button :submit
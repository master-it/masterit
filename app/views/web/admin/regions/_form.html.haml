= simple_form_for @region, :url => url, :html => { :class => 'default_class' } do |f|
  = f.input :state_event, as: :state_event
  = f.input :name
  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to t('.cancel'), admin_regions_path, :class => 'btn'
